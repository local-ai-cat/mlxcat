name: ci

# Build + unit tests on GitHub-hosted Apple Silicon (free for public repos).
# Model-backed tests skip cleanly here (no weights on the runner); they run in
# scripts/nightly-models.sh on our own Macs — see CONTRIBUTING.md § Tests.
# The leaderboard gate is cheap and runs on Linux.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: swift build + test (macOS, Apple Silicon)
    runs-on: macos-26
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - name: Toolchain (log what the image provides — no silent pinning)
        run: |
          xcodebuild -version
          swift --version

      - name: Cache SwiftPM
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/Library/Caches/org.swift.swiftpm
            ~/Library/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Package.swift') }}
          restore-keys: |
            spm-${{ runner.os }}-${{ runner.arch }}-

      - name: Resolve
        run: swift package resolve

      - name: Build (debug, with tests)
        run: |
          set -o pipefail
          swift build --build-tests 2>&1 | tee build.log | grep -E "error:|Build complete" | tail -40

      - name: Test
        # Model-gated suites throw XCTSkip when MLXSERVE_*_TEST_MODEL is unset;
        # Metal-dependent probes skip when no Metal device is visible.
        run: |
          set -o pipefail
          swift test --skip-build 2>&1 | tee test.log | grep -E "Test Suite|Executed|Test run with|error:|failed|skipped" | tail -60

      - name: Summarize
        if: always()
        run: |
          {
            echo "### swift test"
            grep -E "Executed [0-9]+ tests|Test run with [0-9]+ tests" test.log | tail -2 || echo "no summary line found"
            echo; echo '<details><summary>skips</summary>'; echo
            grep -E "skipped" test.log | head -50 || true
            echo '</details>'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: logs-${{ github.run_id }}
          path: |
            build.log
            test.log
          retention-days: 7

  leaderboard:
    name: leaderboard is derived from results
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: LEADERBOARD.md matches bench/results
        run: python3 bench/leaderboard.py --check
      # The leaderboard check only proves the rendered file is not stale. This
      # is the one gate in the repo that can fail because a COMPETITOR is
      # better: every other gate compares mlxcat to its own past, which is how
      # the suite stayed green while we won 6 of 44 cells.
      - name: parity against other engines has not regressed
        run: python3 bench/parity.py --check
      - name: bench scripts parse
        run: |
          python3 -m py_compile bench/run.py bench/leaderboard.py
          python3 -c "import json; json.load(open('bench/engines.json')); json.load(open('bench/matrix.json')); print('engines.json + matrix.json ok')"
          python3 bench/run.py --dry-run --engines mlxcat --model-set smoke --allow-loaded >/dev/null && echo "run.py --dry-run ok"
      - name: harness safeguards (the macOS-only guard tests skip here)
        run: python3 bench/test_run.py

  shell:
    name: shell scripts parse
    runs-on: macos-26
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: for f in scripts/*.sh scripts/**/*.sh bench/*.sh bench/queue/**/*.sh; do [ -f "$f" ] && zsh -n "$f" && echo "ok $f"; done

      # The runaway guard is the one piece of the harness whose failure mode is a
      # dead machine rather than a bad number, so it is tested where it runs:
      # `proc_pid_rusage` is macOS-only and the Linux job above skips those cases.
      - name: harness safeguards (full, incl. the runaway guard)
        run: python3 bench/test_run.py
